apiVersion: skaffold/v4beta13
kind: Config
build:
  local:
    useBuildkit: true
    push: false # shouldn't technically be necessary

manifests:
  helm:
    releases:
      - name: postgres
        chartPath: infra/charts/postgres

deploy:
  helm: {}
